def nsn(nums):
    n = len(nums)
    res = [-1] * n
    stack = []  # stack holds indices

    for i in range(n):
        while stack and nums[i] < nums[stack[-1]]:
            idx = stack.pop()
            res[idx] = nums[i]
        stack.append(i)

    return res

print(nsn([4, 8, 5, 2, 25]))
